Language: python
python: 
  - "3.6"
os:
  - linux

env:
  - num=1

install:
  - sudo pip install --upgrade pip 
  - sudo pip install Jinja2 

script:
  - chmod +x *.sh
  - chmod +x *.py
  - ./build.sh once

before_deploy:
  - git config --local user.name "Hrisanfov"
  - git config --local user.email "Ilya.Hrisanfov@gmail.com"
deploy:
  provider: releases
  api_key: "f6e8e80f64ed8c972a4a7eeb5e0f84b9ca4850b1"
  file: slides.yml.html
  skip_cleanup: true
  overwrite: true
